streamlit>=1.28.0
# Removed heavy or platform-specific packages (numpy, pandas, scipy, pygame)
# Add packages back only if a game explicitly requires them and your deployment
# target supports building wheels for those packages.
